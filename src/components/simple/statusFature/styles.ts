import styled from "styled-components";

export const status = styled.section`
    display: grid;
    grid-template-columns: 85% 36%;
    margin-left: 1.7rem;
    width: 20.3rem;
    height: 2.4rem;
    top: 10 rem;

    font-style: normal;
    font-weight: 400;
    font-size: 1rem;
    line-height: 1.1rem;
`;

export const statusTotal = styled.div`
    width: 20.3rem;
    height: 2.4rem;
`;

export const total = styled.p`
    width: 9rem;
`;
export const statusFature = styled.div`
    width: 6rem;
    height: 1.1rem;
    left: 15.9rem;
    top: 10rem;
`;

export const textStatusFature = styled.p`
    font-style: normal;
    font-weight: 400;
    font-size: 1rem;
    margin-right: 1.5rem;
`;